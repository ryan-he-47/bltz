# 32 — v2 施工计划:AE 先行,双臂对照(2026-09-17)

> 配套 docs/31(v2 设计)。纪律照旧:每阶段先诊断验证再推进;POC 定性口径
> (D10);本机 8GB 只做原型;重型上集群;用户拍板权不变。
> **本计划在用户审阅 docs/31 并答完问询后才动工。**

## S0 — 本机字节串自编码器原型(先行,卡住再往下)

**目的**(用户原意):① 找出"多大 AE 能胜任混合 BPE 短字节串重建"——过大
会过拟合、嵌入空间崎岖难预测(CoSE Table 2:D 越大重建越好但预测与 SC
越差,存在甜点);② 分析嵌入空间形态,给骨干可预测性一个前置判断。

**数据**:本机 `data/cache_dryrun`(不够就拉一个小缓存);Qwen3.5-2B
tokenizer 文件从集群 scp(几 MB,不要权重);增强 BPE 分割器离线预烘焙
(每样本掷一次定型,p=0.5——单遍训练下与在线等价,已拍板)。

**规模网格(已拍板)**:`d_emb ∈ {32, 48, 64}` × enc/dec 宽度 `{256, 384}`
共 6 组,**从 32-256 组起跑**;enc 2 层 inducing 8 不变,**patch 内字节
位置编码拼接保留**(d_byte+d_pos,用户提醒);dec = 条件查询头(film,
hidden 512);每组几万步量级,本机可承受。

**测量**:
- 重建:字节 CE / **exact-match 率**(整串全对)/ EOS 位置准确率;
- 几何:PR / anisotropy / kNN-CC / **SC**(k-means 多 k 均值,CoSE 口径);
- 可预测性旁证:相邻 patch 嵌入的局部线性度( cheap probe,供参考)。

**产出**:选定 AE 配置 + 嵌入几何报告(回填本文);判"空间太崎岖"的
预警信号:exact-match 高但 SC 低、PR 贴近 d_emb 上限、随机串 PR 与真实串
PR 倒挂(对照 v1 的 35-45 经验值)。

**S0 回填(2026-09-17/18,dryrun 池 35,851 条去重串 / val 1,792)**:

| 配置 | val loss | val EM | 备注 |
|---|---|---|---|
| 32-256 | 0.171 | 94.3% | 4.35M 参数;PR 28.4/32、aniso +0.117、SC 0.105;val loss ~3k 步见底后缓升(池小过拟合) |
| 32-384 | 0.178 | 93.8% | 加宽无益(同过拟合) |
| **48-256** | **0.076** | **97.4%** | **明显跃升;宽度 256 已够** |
| 48-384 | 0.082 | 97.1% | 同上 |
| 64-* | — | — | **弃跑**(2026-09-17 用户裁决:GMM 高维拟合难,见 docs/31 §5) |

读法:48 维是当前甜点候选(EM 97.4%,val loss 2.3× 好于 32);宽度
256 封顶;几何整体健康(近满秩、低各向异性——重建塑形把 v1 的窄锥
摊开了)。池子只有 35.8k 条去重串是主要限制,S1 全量缓存后数字还会涨;
CoSE 低维利于预测的先例 + 48/32 的边际收益 = **48-256 定为 v2 AE 基座
(2026-09-18 用户拍板确认)**。注意:S3 用的冻结 AE 需在 S1 全量缓存上
重训(dryrun 池仅 3.6 万条,过拟合边缘);S1 完成后先补一刀 AE 全量训练
(规模同 48-256,步数按池规模定,几十万步量级,V100 上很快)。

## S1 — 集群缓存 v2(增强 BPE)

- 缓存格式:**预烘焙单套分割**(bytes + units,同 v1 形态)——分割在
  构建时一次性定型(p=0.5 合成规则);不存双 mask、不做在线随机
  (单遍训练等价性,docs/31 §5)。
- 全量 10BT 重建(新目录,旧缓存 85GB 是活资产不删;scratch 预算:
  现 ~120GB + 新缓存 ~90GB ≈ 210GB,300GB 配额内,先算后跑)。
- **烟雾验收已过(573155,2026-09-19)**:300 docs/文件 ×14 shard;
  均值 3.47 字节/unit(len=1 占 41%:单符号/单字母/单空格,len 3-8 为主力,
  92.5% ≤8;最长 23,长词完整保留 antidiscrimination 等,em-dash 合并词
  正常;罕见词内部出现 BPE 主张的随机碎切——设计内的多样性)。
  **口径变化注意**:unit 均长 3.47B(v1 ~4.6B)→ S=512 patch 的字节覆盖
  ~1.8KB(v1 ~2.4KB),后续做字节预算对照时记得换算。
- **正式构建:573322 运行中**(parquet 已在 HF 缓存,预计 ~1h);**AE 全量
  重训 573323 挂 afterok 依赖自动接力**(stream 模式,100k 步 ≈ 2560 万条)。
  HF token 已存 `$SCRATCH/hf_cache/token`(600,HF 自动读取,工作区无明文)。
- 验收:抽样对拍——间隙集合 = 两方并集、一致间隙必切、统计分布
  (patch 长度直方图 vs v1)入档。

## S2 — 骨干 + MDN 实现与本机冒烟

**已落地(2026-09-18,commit 待提交见 git log)**:`bltz/models/mdn.py`
(两层非线性缓冲 + 对角 GMM,fp32 NLL,小初始化)、`bltz/models/model_v2.py`
(冻结 AE + 宽非线性适配器 + 骨干 + MDN,BOS 可学习嵌入,λ 硬 detach)、
`bltz/objectives_v2.py`、`scripts/train_bltz_v2.py`(复用 v1 trainer)、
`configs/v2.yaml`(60k WSD)、`tests/test_v2.py`(形状/冻结/过拟合
17.4→-34.2/采样链,绿)。端到端真冒烟(48-256 AE + dryrun 缓存,100 步):
127.5M 参数(可训 123.1M),NLL 82.6→56.1 健康下行,~0.5s/step@batch4
本机。集群 sbatch 已备:`bltz_ae_full.sbatch`(全量 AE 重训,stream 模式,
依赖 CACHE_V2_READY)+ `bltz_v2_60k.sbatch`(依赖 AE full ckpt)。


- `bltz/models/`:AE 模块(Eθ/Dθ 独立可训)、MDN 头(**两层非线性缓冲**
  (768→1024→SiLU→1024→SiLU→K(1+2d),2026-09-18 拍板)+ π/μ/σ fp32
  NLL、σ 下限、输出层小初始化)、骨干**输入适配器**(LN→Linear(48→2048)→
  GELU→Linear(2048→768),已拍板默认,docs/31 §2.3);`BltzLMv2` 组装,
  detach 边界明确。
- 推理循环:BOS → GMM 采样 → 解码到 EOS →(可选)回投影;温度 τ。
- 本机冒烟:小配置过拟合单 batch(重建与 NLL 分别能降到接近 0)、
  采样一条生成链不崩;测试套件新增:test_ae_recon / test_mdn_head /
  test_seg_v2(增强 BPE 合成规则)。
- MDN 卫生:σ=ELU+1+ε;NLL spike 走 spike_skip;记录 π 熵与有效分量数
  (坍缩早期信号)。

## S3 — 骨干 POC(集群,V100;frozen 先行,joint 降级备选)

- **臂 F(frozen,先行)**:S1 全量缓存上重训的 AE 冻结 → 骨干 MDN 训练。
  **2026-09-19 拍板修正:先用 32 维**(配置事故意外产出全量 32 维 AE
  ——ae.yaml 起跑组 32 未改回 48,573889 训出的 best@99000 实为 d_emb=32;
  用户:顺势先用 32 维试水,CoSE 低维先例支持;ckpt 目录已改名
  `ae_32_256_full`,POC 用 `--set model.d_emb=32`;48 维留作升级臂)。
  **验收门:POC 提交前必须先看 `diag_ae_v2` 三件套**(几何/频率分层 val
  ·零次桶/distinct 曲线 + Heaps 外推),`slurm/bltz_ae_diag.sbatch`。

  **三件套验收通过(2026-09-19,32 维 best@99000;本机 --units-pkl 模式
  零额度跑法:登录节点抽样打包 pkl 下拉):**
  - **频率分层 val**:high/mid/low 桶 EM 全 1.0000;**零次桶 n=233,
    EM 93.6% / bacc 96.9% / CE 0.122**——真字符串级泛化成立;
  - **几何**:SC 0.384(超 CoSE 论文最优 0.322)/PR 23.9/32/aniso +0.405/
    kNN-CC 0.349;近邻语义清晰(数字簇/换行簇/长稀有词簇);
  - **distinct 曲线**:V∝n^0.72,13.1B units → 约 **76M 去重串**;AE 全程
    抽取 25.6M 条 ⇒ 每条 distinct 平均见 0.34 次,尾部物理上不可记忆——
    零次桶 93.6% 是纯真泛化,非污染。
  全量输出在 `checkpoints/diag_ae32_full.log`(本机)。

  **POC 60k 首读(574099,2026-09-19;last@60000,本机 eval,eval 集=v2
  缓存抽样序列):**
  - NLL 曲线:-24.4(5k)→-32.8(60k),**衰减尾段(55-60k)仍在上探**
    (-32.38→-32.77)——"早收敛"是假象,是相对增量缩小的观感,曲线未完;
    留出 NLL -32.756 vs 训练 -32.8,**零泛化间隙**(欠拟合区,容量/数据
    是约束)。skips 全场 6 次。
  - 能力:下一 unit argmax 精确命中 **32.7%**(persistence 基线 0.07%);
    混合分布覆盖:任一分量 cos>0.95 命中真值 **60.9%**,top5-π 覆盖
    49.5%——分布在学、排序还嫩;π 熵 1.93,有效分量 4.4/64(无坍缩)。
  - **分锅探针(重要)**:AE 解码器抗噪 0.07σ→EM 99.5%、0.15σ→86%——
    **离流形解码不是瓶颈**;cos(μ̂,λ)=0.64 + snap-to-manifold 无效
    (0.334≈0.336)→ 误差在骨干预测本身。生成碎片的机制:多模态位置
    argmax 分量均值落在簇间 → 解码出非词碎片;升温采样(τ=1.3)反而
    浮出真词("Why is …"/"you will not"),distinct 14→44/64。
  - 口径注意:teacher-forced 字节 CE 25.6 nats 是解码器锐度伪影
    (答错时置信地错),不是 LM 意义上的损失;跨架构基准以 EM/覆盖率为准。
  - 判读:架构链路全通,瓶颈=训练量与排序锐度。**v1 的"幽灵词"病理
    已根治在原理层**(分量=完整词承诺;采样=词级温度)。
  - **1B 长训与 typo-POC 对照(2026-09-22;同协议 diag_v2,eval 同集)**:
    | 指标 | plain@60k | **plain@1B(1.4B 曝光)** | **typo@60k(48d,p=0.1)** |
    |---|---|---|---|
    | 留出 NLL | -32.756 | **-33.821** | -59.323(λ 空间不同不可比) |
    | argmax EM | 32.7% | 34.3% | 34.2% |
    | conf_rerank EM | 35.5% | 36.7% | 35.3% |
    | top5 覆盖 | 48.5% | 51.2% | 49.2% |
    | edit≤2 / >3 | 53.1/34.8 | 53.5/34.9 | 53.3/35.0 |
    | 有效分量 | 4.4 | 4.2 | 5.1 |
    | 生成 conf | -0.63/-0.70 | **-0.23/-0.16** | -0.24/-0.23 |
    读法:① 1B 的 NLL 又磨掉 ~1.0(衰减尾仍在上探,**收敛远未到来**),
    能力小幅上行(+1.5pp EM/+2.7pp 覆盖),排序 gap 未收(51% 覆盖 vs
    34% argmax);② **生成置信大幅改善**(verifier 口径 -0.63→-0.16);
    ③ typo@60k ≈ plain@1B(略逊覆盖),失败模式与 plain 完全一致——
    "误差以 typo 表达"在 plain 下本来就成立;④ typo 臂混合更散
    (5.1 有效分量)符合分离空间给 GMM 更多可放模态的预言。
    **变量隔离**:typo@60k 与 plain 的差异含 typo+d_emb 两变量
    (用户预案)——48 维 plain 解混臂 sbatch 已备
    (bltz_ae_full_plain48 / bltz_v2_plain48_60k),**已挂后按用户
    指示撤回(578881/578883 已 scancel),先看本表结果再定**。
  - **词表吸附解码验证(2026-09-22,用户提出):** GMM 密度在合法词
    (Qwen∪缓存 top-10 万)嵌入表上离散化 = 词级分类分布;
    EM 37.9%(>解码器 34.6%),乱码模式消灭,**首个通顺英文样本**;
    严格版 > 余弦 +3.2pp(σ 投票有效)。`scripts/snap_decode.py`
    入库,覆盖 95.6%(表待扩充)。
  - **snap 三读出对照(2026-09-22,补 plain@60k/typo@60k)**:
    plain@60k gmm 36.0% | **typo@60k gmm 36.5%(同步数 +1.5pp)** |
    plain@1B gmm 37.9%;读出与 AE 无关可互换。生成:两臂同层
    (功能词流利/内容浅),低 τ 出真句子,高 τ 现引号吸引子与
    数字汤。**typo-2B 计算最优长跑已挂**(579081:50k 分叉,
    LR 2e-4,+244140 步 ≈2.0B units ≈30h)。
  - **字节级匹配与推理侧重排(2026-09-20,diag_v2 扩展;口径:字节
    Levenshtein,NED=edits/max(len),prefix%=最长公共前缀占比)**:
    | 读出 | EM | 中位编辑 | NED | edit≤2 | edit>3 |
    |---|---|---|---|---|---|
    | argmax-π | 32.7% | 3.0 | 0.78 | 48.0% | 43.7% |
    | min-σ(top5) | 35.1% | 2.0 | 0.61 | 52.9% | 34.8% |
    | 解码器置信重排(top5) | 35.5% | 2.0 | 0.60 | 53.1% | 35.1% |
    **失败模式以错别字级为主**(过半预测 ≤2 编辑;长尾巴=短串上选错词);
    min-σ ≈ 解码器置信(σ 即自信度代理,min-σ 免解码更便宜);重排是
    免重训的推理侧收益。生成仍受复合误差拖累;解码器自洽置信可作
    verifier(生成串 -0.63 vs 真串 ~0)。top5 解码候选含真值率 48.5%。
  - **语义结构诊断(2026-09-20,diag_v2_semantic.py;λ 是字形空间,语义
    只可能住在 768 维 h 里)**:
    - **h 聚类存在且是语义/语法簇**:功能词簇(lower 纯度 0.97:
      on/of/to/than/without/in/and/through)、句首连接词簇(However/
      This/Moreover/Thus/To)、话题名词簇(Information/Server/deployed/
      efficiency/code)、后缀碎片簇(olar/rons/tons/oted);对照 λ 空间
      NMI 0.394 vs h 0.394→**0.142**(h 不太管表层字形),h 簇内归一化
      编辑距离 0.768 vs λ 0.643(**h 簇拼写上更散——正是"语义相近拼写
      不同"的签名**)。
    - **高置信大错案例分两类,可用解码器置信完美分开**:(a) 真词级
      语义邻近错误(conf≈0,如 'of our public _' → 'health'——
      "public health" 是合理联想);(b) 碎片分量(conf≪-0.5 的
      ' gyvBi…'——介于词间的分量均值,推理时可用 conf 阈值直接滤掉)。
      120 案例在 `checkpoints/semantic_cases.txt`;t-SNE 在
      `viz/h_tsne.png`。

  **POC 首发起死事故与根因(574046,2026-09-19)**:起跑后 loss 33.9→
  28.8 健康,~84 步起 gn 冲 Infinity(fp16)/3000+(bf16 同样复现)→
  spike_skip 全场拦截(thresh floor 2000,中位数冻结)→ 死锁。**根因 =
  docs/31 §4 预言的 MDN σ 坍缩**:分量一旦开始拟合,σ 冲向 1e-3 下限,
  NLL 梯度按 1/σ、1/σ² 放大 → 爆炸。这不是 v1 CE 尺度的"尖峰",是 MDN
  的固有力学;守卫 floor(2000)按 v1 CE 梯度标定,对 v2 不适用。
  **本机 A/B 定案**:sigma_floor=0.1 时 300 步 NLL 34.0→-7.07、gn 稳定
  60-90、零跳过;floor=1e-3 时 step 125 起爆。**教训:NLL 是密度,
  合法取负值;loss 下探时盯 gn 而非 loss 符号。**
  **拍板(2026-09-19):sigma_floor=0.05**(表达力优先;本机验证 300 步
  NLL→-12.1、gn 峰值 176、零跳过)。deepseek 四手段盘点:logsumexp 与
  梯度裁剪**已自带**;softplus 与 ELU+floor 渐近等价不换;logσ 参数化
  有价值非必需,floor 已根治病根,暂不引入(Occam)。
- 规模(2026-09-18 用户拍板,激进试水):**直接 60k 步**(不再 20k 先读),
  WSD 调度,势头不对随时早停(里程碑 ckpt 保留分支能力);batch 16 /
  LR 4e-4 沿用 v1 配方。
- ~~臂 J(joint-decoupled)~~:**降级为备选**(2026-09-17 拍板)——frozen
  效果特别好就万事大吉;不佳再上 joint(AE+骨干同步、梯度硬切断、
  双 Adam 组),用于区分"嵌入空间冻结错位"与"联合震荡拖垮"两种病因。
- 规模建议:20k 步 batch 16 先读(每臂 ~6h),健康再上 60k;LR 沿用
  4e-4 WSD;两臂各一份 diag。
- 读数:字节 CE(解码口径)+ exact-match + 生成定性 + π 熵/分量使用率;
  对照锚 = v1 各 run(2.50 底线、k1-mix 2.173)。
- CoSE 动态观察(用户标注低可信度)仅作排查线索:F 臂若骨干不适配
  (NLL 不动)→ 查嵌入空间冻结错位;J 臂若骨干被噪声嵌入拖垮 →
  查 AE 早期震荡;两臂都不动 → 回 S0 审嵌入空间。

## S4 — 诊断改造与报告

- diag_poc 分化 v2 版:重建/exact-match/几何/承诺度指标;生成页加
  distinct-n 与 τ 扫描(词级温度第一次有意义,值得专门一节)。
- 报告口径:字节 CE 为唯一跨架构基准;v1 全部数字留档不溯改。

## AE 补救工具箱(2026-09-19 用户备录)

> 触发条件:三件套验收(几何/频率分层 val/distinct 曲线)异常,或 POC
> 骨干 NLL/生成读数不理想。四条都是 AE 数据侧的改动,不动骨干。

1. **去重词库数据集**:全数据集 unique patch 去重成词库,平衡高频/低频
   串的训练信号——拉平 Zipf,嵌入空间会把容量从头部挪向稀有串。风险:
   稀有串里混着随机碎切产物与噪声串,平衡可能把容量浪费在垃圾上;且
   特征提取器是否该偏离部署分布,无先验答案。用户注:会改变空间形成
   方式,好坏未知。
2. **typo 簇增强**:不仅重建真词,还重建含微小 typo(错字/缺字/重复/
   交换)的 patch,**重建目标是 typo 串本身(不是去噪回真词)**——把
   一个数据点变成围绕正确词的密集点云。双重收益:(a) 潜空间局部致密化;
   (b) 骨干预测嵌入稍有偏差时,落进 typo 簇 → 解码成近似串,而不是
   彻底 OOD 崩溃(误差以 typo 形式柔和表达)。代价:簇变宽,半径过大时
   近形词可能互相渗流。
   **→ 已立项(2026-09-20,本机实验,数据=本地 1-parquet 缓存 338M
   units,d_emb=48——typo 抬升信息量,事后用有效秩复核维度;用户拍板)**:
   - p_typo 网格 {0.1, 0.2, 0.4} + plain 对照(p=0),四臂串行本机;
   - 操作混合:替换40/删除20/重复15/交换15/大小写10;编辑数 1(85%)
     或 2(15%);
   - 字节边界:ASCII(32-126)字节级操作;≥0x80 多字节 UTF-8 字符**整体
     视为不可切分单元**(只整体删除/重复)。**注意事项(用户备录):中文等
     多语言的 typo 模式不是简单拼写错误,多语言语料的增强模式以后详细
     商讨;现在战场在 FineWeb 英文,先不考虑。**
   - **p_typo 频率/长度自适应(用户提出,我实现)**:p_eff = p × L(len)
     × F( freq);L:<3→0,3-4→0.5(仅替换/大小写),≥5→1;F:训练前
     256 序列预统计,top-3 万高频串 ×0.25,其余 ×1。开关
     `typo.freq_throttle`,失灵即退回固定概率。
   - 验收对照:抗噪曲线右移(核心)/高噪声下错误串编辑距离集中 1-2/
     typo 变体与源词余弦排名/几何(kNN-CC 升、SC 可适度降、PR 不塌)/
     clean val EM 不低于对照臂 2pp。
   **→ 教训纠正(2026-09-21,用户指正,撤销此前"结案"判词)**:
   **typo 不是 unwanted 噪声,是将嵌入空间从稀疏点变为密集簇的结构性
   改善**——(a) 真实词周围环绕各自的 typo 点云,**拉开真实词之间的最小
   距离**(plain 中形状过似的真词会挤在一起,typo 云创造间隔,减轻骨干
   消歧压力);(b) 嵌入误差以 typo 形式柔和表达而非 OOD 崩溃——为骨干
   预测校准与字节串纠错算法提供着力点;(c) 为辨识错别字、拼写与单词
   计数、理解合成词/自造词等下游任务**预留嵌入结构抓手**。
   **我的错误三条**:① 用"λ+高斯噪声→解回真词"的去噪口径测一个
   "typo 忠实重建"的设计,南辕北辙;② 把"变体离源词变远"(点云成形
   的直接证据)读反——数据里 cos-to-source 随剂量 0.746→0.736→0.725
   →0.718 恰是 typo 越来越被感知、与原词有区分度的**正确签名**;
   ③ 凭先验拍两档高斯噪声当评估手段,正是当初被否掉的乱加先验。
   **训练侧落实是对的**(被污染串同进 input 与 target,目标=typo 串
   本身,非去噪)。   **重评估口径(数据驱动)**:typo 等效噪声 σ_typo
   (变体-源词在 λ 空间的位移谱)/ **typo 保留率**(encode(typo)→
   decode 对 typo 目标的忠实度,及 snap-回源词的"吸引失败"率)/ 以
   σ_typo 标定的分档噪声下 failure 行为模式(exact/typo 级/远真词/
   碎片四分类)——而非任何"解回真词"指标。
   **→ 重评估结果(2026-09-21,diag_ae_typo 重写后,四臂对照):**
   - **typo 保留率单调随剂量上升**:plain 0.876 → t01 0.947 → t02 0.963
     → t04 0.977;case 类从 0.583(snap 4.7%)修到 0.92+(snap 0.7%)——
     点云成形且与原词有区分度的微观证据;
   - 点云半径:t04 最大(2.32 vs plain 2.06)——保真能力越强云铺得越开;
     但 t04 高噪端 garbage 回升,**剂量过大开始渗流**;
   - **t01 为失败模式甜点**:2σ_t 处 garbage 0.465 vs plain 0.627(-26%)、
     exact 0.280 vs 0.113(2.5×)——误差以 typo/真词柔和表达,校准着力点
     兑现;
   - 留白:真词分离(C)指标退化(高频真词 NN 距离贴 0,被分割变体主导,
     需换度量);本地单文件缓存单种子;**端到端收益(骨干误差校准)仍需
     typo-AE+骨干实跑验证**(下一步候选)。
   **→ 真词分离度量修复与新读数(2026-09-21)**:旧协议对"高频真词"集
     算最近邻距离,被同一词的分割表面变体('the'/' the'/'The')钉死在 0。
     修复两段:**C1 规范形去重队列**(strip+lowercase 去重、len≥4,n=405)
     量真·词间间隔;**C2 点云余裕**(margin = 最近他词距离 − 点云半径,
     余弦单位统一)与**碰撞率**(变体离他词比离源词更近的比例)。
     读数:**t01 全面甜点**——margin 最佳(+0.071)、负余裕率最低(0.418)、
     碰撞与 plain 持平(0.311),且保留率 0.947;C1 间隔 p1 端随剂量拉开
     (0.0515→0.0613,t04 +19%)但 t04 碰撞恶化至 0.371——**分离收益与
     渗流代价的剂量曲线交叉在 t01 附近**。
   **→ 变体可分性排查(2026-09-21,"架构漏洞?"之问;探针=本地 48 维臂,
   无关配对对照)**:
   - 旧协议退化=度量样本集缺陷,非模型缺陷(分身近邻钉死 NN 距离);
   - **输入端**:大写方向存在(+0.425 vs 基线 +0.075,平均方向对齐 +0.654);
     复数方向中等(+0.201 vs +0.017,对齐 +0.447);前导空格无单轴(弥散
     编码,边界标记而非语义轴)。语义/语法属性以线性方向承载于 λ 空间;
   - **输出端**:自然表面形式分辨率 ~完美——'The'/' watch'/'phrases'
     各频次档(含零次)decode(encode(x))==x 均 0.97-1.00;早前 case-fid
     0.583 系**非自然形态**(词中乱序大小写,文本中不存在)向常见形的
     纠错式坍缩,无害且有利;typo 增强修到 0.92+ 属锦上添花;
   - 中点解码=近形混合或干净选边,非 OOD 崩溃。**结论:无架构漏洞**;
     语义相关差异输入有方向、输出有分辨率,非语义差异坍缩掉。
     (待办:同法复核集群 32 维 plain-AE 一次。)
3. **合成词 val 探针**:拼写合理但真实文本中极难出现的伪词(如
   'blorft'、'draction'),直接测字符串级泛化(=频率分层 val 的零次桶
   的人工强化版),量化过拟合程度。成本极低。
4. **随机切分增强回归**(待定):v1 式随机切/合 patch 可极大提升数据
   丰度;顾虑是低质量碎片(单字节、断 UTF-8 序列)过多阻碍训练。若启用
   需加质量下限(最小长度、不在多字节字符内部断)。到时候再议。

## 阶段门

S0 不过(AE 重建不过关或空间无可救药)→ 停工回议,不硬闯 S1+。
S3 两臂都显著劣于 v1 锚 → 架构判断复盘会,不是工程加力。

## 待拍板清单(问询后回填)

已答(2026-09-17):预烘焙分割、p=0.5、原始向量+LN(AdaLN 思路内化)、
分量内只取 μ_k、frozen 先行、S0 六组网格从 32-256 起跑。
剩余随文档审阅确认:回投影默认开、去噪解码开关(默认关)、POC 20k 先读、
l_max=32。

## typo-2B 验收(2026-09-24,job 579081)

50k 分叉 +244140 步(总 294140,~2.0B units),36.7h COMPLETED。曲线教科书
WSD(稳定段 -59→-61 缓磨,~180k 起退火下探至 -63.41;gn 缓爬 6→10,
skips 91/294k ≈1/2700 步,无失控)。`viz/v2_typo2b.png`。

| 指标(docs/34 协议) | typo@2B | typo@60k | plain@1B |
|---|---|---|---|
| held-out NLL | **-62.24** | -59.3 | -33.86(32d 空间,不可比) |
| argmax_pi EM | 0.369 | 0.342 | 0.343 |
| conf_rerank EM | **0.372** | — | — |
| top5 候选含真值 | 0.531 | — | 0.512 |
| edit<=2 | 0.514-0.548 | ~0.53 | ~0.53 |
| snap gmm EM | **0.390** | 0.365 | 0.379 |
| snap 覆盖 | 0.956 | 0.956 | 0.956 |
| π 健康 | H=1.98, eff 4.9/64, top1 0.42 无坍缩 | 同层 | 同层 |

生成(snap 读出,多 τ):τ0.6-0.8 出完整真句("or to make a new way to
use. The first time to use...", "the area of the 1st century ... The 18th
century is a good"),高 τ 仍是引号/数字吸引子汤;内容依旧浅——2B 规格
把"内容空洞"考卷的分数提高了但未及格,缺口在能力侧深度(下一档=数据
×5 或参数档升级,待拍板)。语义结构(diag_v2_semantic):h 空间话题簇
成立(c41/c06/c24 编程话题簇、c11 句首连接词簇、c39 功能词簇纯度 0.97),
NMI(h,type)=0.151 < NMI(λ,type)=0.405 符合分工设计;16288 位置、120 例
失败案例倾倒 checkpoints/semantic_cases.txt。
诊断日志:checkpoints/diag_v2_typo2b.log / snap_decode_typo2b.log /
diag_v2_semantic_typo2b.log;ckpt 本地副本 Temp\opencode\v2_typo2b_last.pt。

## snap-PPL 四点 scaling 读数(2026-09-24,snap_decode --ppl,64 seqs,校准 T=10)

词级 PPL / bits-per-byte(表=Qwen∪cache-top10 万,OOV 均匀回退 logN=12.44,
同一 eval 集,双臂同口径可互比——表字符串集一致,覆盖同为 0.9553):

| ckpt | units | gmm EM | PPL(校准) | bpb(校准) |
|---|---|---|---|---|
| plain@60k | 0.49B | 0.357 | 2156 | 3.166 |
| plain@1B | 1.00B | 0.370 | 1875 | 3.109 |
| typo@60k | 0.49B | 0.361 | 1302 | 2.958 |
| typo@2B | 2.41B | 0.382 | **954** | **2.830** |

读数:① 两臂斜率均 ≈ **-0.056 bpb / 数据翻倍**,无饱和;② **bpb 口径下
typo 臂端到端增量显形**(typo@0.49B 已胜 plain@1.0B,EM 口径曾低估);
③ 外部锚(语料不同,粗参):GPT-2 117M zero-shot enwik8 1.16 bpb;
GPT-2 124M OpenWebText 复现(27.5B tok)bpb≈0.93;BLT 1B@100B bytes
~0.85;Chinchilla 拟合 124M@~2B tok ≈1.1 bpb——我们绝对水平落后 ~2.5×,
仪器保守性(单 T 校准/表碎片化/OOV 均匀回退)贡献未知部分;
④ **原始(T=1)分布严重过自信**(bpb 11.6-14.4,比均匀字节还差),
T=10 一致最优——GMM 密度离散化的校准属性,入档。

## TinyStories MDN 头对照实验(2026-09-24 用户拍板设计)

**动机**:GMM 输出层宽 K(1+2d)=64×97=**6208**,而两层缓冲只有 1024——
瓶颈假说:头部宽度限制了 GMM 表达力(π/μ/σ 的 6208 维全从 1024 维挤出)。

**三臂**(同一冻结 typo-AE(ae_48_256_typo_full,与 typo-2B 相同),
同数据同预算,一次回答三个问题):
| 臂 | 头 | 头参数量 | 回答 |
|---|---|---|---|
| ts_base(对照) | 768→1024→1024→6208 SiLU-MLP | 8.2M | 基准 |
| ts_wide | 768→6208→6208→6208 SiLU-MLP | ~82M | 头宽度是否瓶颈 |
| ts_swiglu | 768→6208→6208→6208 SwiGLU 块 | ~164M | 网络种类是否还有空间 |

**数据集**:TinyStories(roneneldan/TinyStories train,4 parquet,~0.5GB 文本,
简单语料控制变量——顺便回答"性能差是模型上限低还是数据复杂;数据够简单
能否展现及格语言水平")。缓存走增强 BPE 同一管线(parquet 重命名复用
build_cache_v2,零代码改动),评估 pkl 用新 scripts/dump_eval_pkl.py
(login 节点可跑)从该缓存采样——**三臂评估必须用这个 TinyStories pkl**,
别拿 FineWeb 的 diag_sample.pkl。

**训练口径**:40k 步(≈2 epoch,WSD decay_frac 0.4,LR 4e-4 同 POC),
batch 16,三臂统一 grad_ckpt=true(宽臂显存保险;步时三臂间可比,
与历史 FineWeb run 不可比——无所谓,本实验只看臂间差异)。
slurm: bltz_ts_cache.sbatch(先跑)→ bltz_ts_{base,wide,swiglu}.sbatch。
**SwiGLU 臂参数量是 base 的 20 倍**(同宽度规格下 3 矩阵的固有不对称),
解读时记住:若 swiglu≈wide 则网络种类无差;若 swiglu>wide 需区分
"种类红利"与"纯参数量红利"(可后补等参对照)。

**验收指标**:snap-PPL/bpb(TinyStories 词表小,覆盖应近 100%,PPL 干净)
+ gmm EM + 生成样本。若 base 在 TinyStories 上能到 bpb ~1 量级且生成通顺
小故事,则"能力上限"假说被削弱,FineWeb 上的 2.83 bpb 含数据复杂度成分。

## TinyStories MDN 头对照:结果与结论(2026-09-24)

三臂 40k 步全部 COMPLETED(base 3.4h / wide 4.1h / swiglu 5.3h;起步 inf
尖峰均被 spike_skip 兜住后平息)。注意:40k 步 = 0.52 epoch(缓存实测
636M units、77.6k 步/epoch;设计时按 ~150M units 误估"≈2 epoch",
实为单遍内对照预算,数据尚有一倍余量)。同一 AE,NLL 首次跨臂直接可比。

| 指标 | base(1024) | wide(6208) | swiglu(6208) |
|---|---|---|---|
| train NLL | -77.91 | -78.98 | **-79.54** |
| held-out NLL | -78.05 | -79.02 | **-79.11** |
| snap gmm EM | 0.4597 | 0.4690 | **0.4755** |
| 校准 PPL(T=10) | 161.0 | **145.0** | 150.5 |
| 校准 bpb | 2.4511 | **2.4006** | 2.4186 |
| top5 覆盖 | 0.728 | 0.739 | **0.759** |
| π 有效分量 | 3.2/64 | 3.2/64 | 3.0/64(更锐,未坍缩) |
| 覆盖 | 0.9903(TinyStories 词表小) | 同 | 同 |

生成(同 prompt 多 τ):三臂均产出**语法完整的 TinyStories 式故事**——
完整句子、对话结构("I can help you!" / "Thank you!")、叙事词汇就位;
与 FineWeb 时代的引号/数字吸引子汤相比是质变。内容重复度仍高(寄存器
所限)。样本全文在 checkpoints/snap_ppl_ts_{base,wide,swiglu}.log。

**三问三答**:
1. **头宽度是瓶颈吗?是真瓶颈但温和**。加宽 6×:EM +0.9pp、PPL -10%、
   top5 +1.1pp——若 1024 是主瓶颈应是数量级变化,实际只有 5-10% 相对。
2. **SwiGLU 有种类红利吗?没有**。2× 头参数换 EM +0.65pp 且 PPL/bpb
   反而略差;种类与纯参数都无实质红利。
3. **数据复杂度 vs 模型上限?各打五十大板后收敛**。定性上简单语料确实
   解锁了"及格语言水平"(故事结构),削弱纯上限论;但定量上 bpb 2.40
   距同档 token 模型在 TinyStories 的可达水平(~0.5-0.8)仍差数倍,
   读出层/头/数据复杂度逐一排除后,**瓶颈收敛到骨干容量×训练量,以及
   GMM 量化读出的固有锐度损失(校准 T=10 才正常是结构属性)**。

**推论**:宽头不建议升默认(82M 换 +0.9pp 性价比差;若要,中间档
2048-3072 测边际即可);下一个杠杆回到骨干规模与训练量。

## 输入/输出侧几何与 σ 诊断(2026-09-24;含一次测量事故与翻案)

**事故记录**:diag_v2_input.py / diag_v2_output.py 初版漏写
`model.load_state_dict(v2_state["model"])`,两轮结论("σ 死通道钉 1.05"、
"h 坍缩 cos 0.89"、"adapter 放大各向异性")全部跑在随机初始化的骨干上,
**作废**。用户以"NLL=-62 与 σ=1.05 不可能并存(下界 46.4)"证伪了第一次
结论,复查定位。教训已立:ckpt 诊断脚本必须带加载 canary(打印 step +
权重块范数)。三个老脚本(diag_v2/snap_decode/semantic)加载齐全,历史
读数不受影响。

**修正后的真实读数**(typo-2B,diag_sample 评估集):

| 位置 | mean cos | eff-rank | 判定 |
|---|---|---|---|
| λ 48d(去重词种) | 0.187 | 27.9/48 | 健康 |
| adapter 768d(去重) | **0.123** | 31.7(≈48 上限的 66%) | **健康,训练后各向异性不升反降** |
| h 768d(骨干输出,位置加权) | 0.140 | 124.2/768 | 健康 |

**σ 分布(真)**:原始地板占用 **0.828**(p5-p75 全=0.0500);
π 加权 p50=0.05;argmax-π 分量地板占用 0.796;max 8.15。
**地板焊死——模型在 83% 的(位置,分量,维度)上想要 σ<0.05**。
但 margin:σ=0.05 已仅为词间距(去重 NN L2 1.05)的 1/20,
**词辨别早已进入 δ 函数 regime**——模型想更锐是连续 NLL 目标在推
(μ 已在词盆内,z²≈30,锐化继续降 NLL),不是词级任务所需。
预判:σ_min 调小(如 0.01)的退火会改善原生 NLL、EM/PPL 基本不动;
该实验便宜(milestone 分叉,低 LR),可作为确认性实验排队。

### 变体云内间距 vs σ 地板(2026-09-24,用户指正后的正确尺度)

全局词间距(1.05)不是 σ 问题的任务尺度;变体云内部(edit<=2:前导
空格/大小写/后缀/形近)才是——失败模式 edit<=2 占 53% 正发生在这层。
实测(3000 锚 × top15 NN,253k 表):edit=1 对 p10 0.273/p50 0.870;
最近 edit<=2 变体 **p10 0.143 / p25 0.285 / p50 0.641**。
σ=0.05 对最紧一成仅 ~8:1 辨别赔率(不够),p25 起碾压;赢分量 μ 距真值
~0.28=云内变体间距,μ 被迫停在变体之间覆盖——地板在任务尺度上确实绑着
最难的一成。**σ_min 退火(0.01-0.02)从确认性升级为有真实增益空间**:
预测 edit<=2 混淆尾改善、EM 小涨、校准更差(T 10→30)。

### σ_min 退火对照:结论(2026-09-24,584944/584945,各 15k 步自 290k 分叉)

| 指标 | typo-2B 原版 | sig05 对照 | sig01(σ_min 0.01) |
|---|---|---|---|
| held-out NLL | -62.24 | -62.32 | **-110.20** |
| gmm EM | 0.382 | 0.382 | 0.372 |
| edit<=2(gmm) | 0.546 | 0.547 | 0.539 |
| top5 含真值 | 0.531 | 0.534 | 0.527 |
| 校准最优 T | 10 | 10 | **30** |
| 校准 PPL / bpb | 946 / 2.83 | 946 / 2.83 | **4817 / 3.50(恶化)** |

三条预测对账:校准恶化 ✓(T 10→30);edit<=2 改善 ✗(不升反微降);
EM 小涨 ✗(-1pp)。**判定:σ_min=0.05 不是学习效率瓶颈;松地板买到
-48 nats 原生 NLL、词级指标全线走平或变差**——密度 NLL 与词级任务之间
存在结构性鸿沟,σ 是白捡 NLL 的正交通道。地板维持 0.05,σ 问题关闭。
(附注:此前"83% 贴地板"的正确读法是 NLL 目标在利用这个便宜通道,
而非能力受限。)
