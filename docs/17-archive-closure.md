# 17 — 项目封存说明(归档收尾)

> **决定**:2026-09-14,用户拍板**停止研发、封存归档**。
> **用户评估(原话大意)**:预期中的原创性已被外部工作砍得撑不起一个 workshop;继续投入将滑向学术 slop,故封存。
> **本档用途**:封存事实的权威记录 + 资产索引 + 遗留事项。自此 `docs/` 冻结不再更新(勘误除外)。

---

## 1. 封存结论

bltz 的三大主张均已被外部工作**独立实现/实证**:

| 主张 | 外部覆盖 |
|---|---|
| (a) 无词表层级字节 I/O | HAT(2501.10322)及其后继(2603.15953,7B/70B,HF 发布) |
| (b) 鲁棒/可迁移 | 同上(HAT 的鲁棒性扰动、跨语言续训实验) |
| (c) 并行局部字节解码 | Fast BLT-D/DV(2605.08044,块扩散+验证)、DMBP(2608.15454,段对齐掩码边际) |

未被覆盖的仅剩 **formulation 级差异**((h,Δ) 场头、空格自停+确定性分割、字节级 MTP 训练动力学诊断、Set 编码器+增广),不足以支撑独立论文(清单与理由:`docs/16` §4、`docs/14` §6)。

## 2. 证据链(封存依据)

- `docs/14` — 范式普查(24 模型,2018→2026)+ 并行解码/多字节预测谱系 + "层级×并行"缺口裁决;
- `docs/15` — HAT 全文深挖(逐行引文)+ HAT 团队 Outlook 兑现审计;
- `docs/16` — DMBP 精读 + 解码范式对比(机制表/架构品味/应用优劣势)+ bltz/HAT/DMBP 三方相似度;
- `docs/13` — MTP 冲突先例档案;`docs/12` — gcos 负余弦第二意见;`docs/10` — 20k POC 诊断。

## 3. 资产索引(封存内容)

| 类别 | 位置 | 说明 |
|---|---|---|
| 设计/施工 | `docs/01`, `docs/02` | 设计思想(含 D1-D11 决策记录)、施工计划 |
| 管线与诊断 | `docs/04`-`docs/10` | 冒烟、D-1 v2、twin 标定、代码架构、runbook、20k POC 诊断 |
| 调研与裁决 | `docs/03`, `docs/11`-`docs/16` | 文献调研、gcos 修正、先例档案、范式谱系、HAT/DMBP 深挖 |
| 用户立场 | `docs/blt_research/ByteField用户立场归档.md` | 工作定位、学术生态位、设计哲学 |
| 代码 | `bltz/`, `scripts/`, `configs/`, `slurm/`, `tests/`(9 个全绿) | 模型/训练/缓存/诊断/集群脚本 |
| 文献文本版 | `reference_projects/*.txt` | BLT / Fast BLT / HAT / DMBP(**不在 git 跟踪内**,见 §5) |
| POC 模型 | 本机与集群(不在 git) | 20k ckpt、60k run 产物以集群侧为准 |

## 4. 已执行的收尾动作

- 新增本档 `docs/17`;状态回写:`README.md`、`docs/01`(封存声明+§10 追记)、`docs/09`(路线图冻结);
- `AGENTS.md` 的封存回写已**撤回**(2026-09-14,待主会话复核后另定);
- 提交:封存 `0d16fbc`(docs/11-17 入库+回写);AGENTS.md 撤回见其后继提交。

## 5. 遗留事项(本机未执行/无法执行)

- **集群侧**:若 60k 长跑(job 547403)仍未结束——按 `docs/08` 优雅中断(`<ckpt_dir>\STOP` 或 Ctrl+C,即存即退)或任其跑完保留 best+final;随后清理 scratch 数据/缓存/中间 ckpt 以释放配额(home 仅留源码与最终 ckpt 的约定不变)。
- **备份提醒**:`reference_projects/`(含 BLT/HAT/DMBP/Fast BLT 文本版)与模型 ckpt 均**不在 git 跟踪内**,如需长期保存请以文件拷贝方式另行备份。
- **外部调研工件**(opencode 工具输出目录)为临时文件;长期版即 `docs/13`-`docs/16`。

## 6. 若他日重启(非预期)

入口:`docs/16` §4 的 formulation 残余清单(Δ 场头 vs LCA 头的同规模 A/B、字节级 MTP 训练动力学诊断)。需新算力与新目标,预期收益不确定。

(完)
